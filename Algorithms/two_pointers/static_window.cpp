ll l = 0, r = 0, k;

// starting window size k
while(r < k){
    // add a[r]

    ++r;
}

// Process window [0, k - 1]

while(r < n){
    // delete a[l]
    // add a[r]

    // Process window [l, r - 1]

    ++l;
    ++r;
}
